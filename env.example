# ไฟล์นี้คือตัวอย่าง — ให้ copy เป็น .env แล้วแก้ไขค่าจริง
# สร้างไฟล์ชื่อ .env ที่ root ของโปรเจกต์ (ข้างๆ package.json)

# 🔑 รหัส PIN สำหรับเข้าหน้าหลังบ้าน (AdminScreen)
VITE_ADMIN_PIN=9999

# ⚠️  อย่า commit ไฟล์ .env ขึ้น Git — เพิ่มใน .gitignore
